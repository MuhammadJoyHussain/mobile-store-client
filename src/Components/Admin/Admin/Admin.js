import React from 'react';
import Sidebar from '../Sidebar/Sidebar';

const Admin = () => {

    return (
        <div>
            <Sidebar />
            <h1 style={{marginLeft: "30%"}}>Welcome to the admin panel</h1>
        </div>
    );
};

export default Admin;